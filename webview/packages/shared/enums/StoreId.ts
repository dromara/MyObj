/**
 * Store ID 枚举
 * 统一管理所有 Store 的 ID，避免硬编码字符串
 */
export enum StoreId {
  User = 'user-store',
  Auth = 'auth-store',
  App = 'app-store',
  Layout = 'layout-store',
  TagsView = 'tags-view-store'
}
